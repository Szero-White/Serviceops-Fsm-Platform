/**
 * Compatibility export retained for branches/bookmarks that still import the
 * former marketing section name. The public page now presents deployment
 * stages instead of unverified pricing claims.
 *
 * @deprecated Import `DeploymentSection` directly in new code.
 */
export { DeploymentSection as PricingSection } from './DeploymentSection'
